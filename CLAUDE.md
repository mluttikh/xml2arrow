# xml2arrow — AI contributor guide

The canonical guide for all AI tools lives in `AGENTS.md`; the import below
inlines it. Do not add project instructions here — edit `AGENTS.md` instead
so every AI tool (Claude, Copilot, Gemini, Codex, Cursor, …) stays in sync.

@AGENTS.md
